import { StyleSheet } from "react-native";

export const styles = new StyleSheet.create({
    content : {
        alignItems : "center",
    },
    avatar : {
        marginTop : 40,
    },
    identity : {
        color : "#fff",
        fontSize : 26,
        fontWeight : "400",
        marginTop : 20
    },
     email : {
        color : "#fff",
        fontSize : 16,
        marginTop : 10,
        opacity : .6,
     }
});