import { StyleSheet } from "react-native";


export const styles = new StyleSheet.create({
    content : {
        alignItems : "center",
        marginTop : 10,
    },
    identity : {
         color : "#fff",
         fontSize : 26,
         fontWeight : "bold",
         marginTop : 20,
    },
        email : {
         color : "#fff",
         fontSize : 16,
         opacity : .6,
         marginTop : 10,
    }
})